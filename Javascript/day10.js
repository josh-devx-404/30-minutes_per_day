// Day 10:

let word = "Josh";
let wordsplit = word.split("", 4);

console.log(wordsplit);

// string.split(separator, limit)
// A string or regular expression to use for splitting. If omitted, an array with the original string is returned.
// An integer that limits the number of splits. Items after the limit are excluded.
